# SaaS 系统学习与开发计划清单

本项目的终极目标是从零构建一个基于 **Next.js + NestJS + PostgreSQL + Redis + Docker + AI Agent** 的 SaaS 系统。路线采用“边学边做”的方式：每个阶段都要产出一个可运行、可验证的小成果，同时补齐对应技术栈的核心概念。

## 总体方向

- [x] 使用 Monorepo 管理前端、后端和共享代码。
- [x] 前端使用 `apps/web`，技术栈为 Next.js + TypeScript。
- [ ] 后端使用 `apps/api`，技术栈为 NestJS + TypeScript。
- [x] 共享包放在 `packages/*`，用于类型、工具函数、配置等。
- [ ] 本地基础设施使用 Docker 管理 PostgreSQL 和 Redis。
- [ ] 数据库 ORM 默认使用 Prisma。
- [ ] 先搭建 SaaS 基础骨架，再逐步加入 AI Agent 能力。

## 阶段 1：项目初始化

### 学习目标

- [x] 理解 Monorepo 的作用，以及为什么前后端项目可以放在一个仓库中。
- [ ] 理解 Next.js、NestJS、TypeScript 在项目中的职责边界。
- [x] 理解 package manager、workspace、脚本命令的基本概念。

### 开发任务

- [x] 初始化 Git 仓库。
- [x] 选择并初始化 Node.js 包管理工具，推荐 `pnpm`。
- [ ] 创建 Monorepo 目录结构：
  - [x] `apps/web`
  - [ ] `apps/api`
  - [ ] `packages/config`
  - [ ] `packages/shared`
- [x] 创建 Next.js 前端应用。
- [ ] 创建 NestJS 后端应用。
- [x] 配置 TypeScript。
- [x] 配置 ESLint。
- [ ] 配置 Prettier。
- [ ] 添加根目录常用脚本：
  - [x] `dev`
  - [x] `build`
  - [x] `lint`
  - [x] `test`

### 验收标准

- [x] 根目录可以统一安装依赖。
- [x] `apps/web` 可以启动 Next.js 开发服务。
- [ ] `apps/api` 可以启动 NestJS 开发服务。
- [ ] 前后端都可以通过 TypeScript 编译。
- [x] `lint` 命令可以正常运行。

### 推荐练习

- [x] 手动画出 Monorepo 目录结构，并标注每个目录的职责。
- [ ] 记录 Next.js 与 NestJS 分别解决什么问题。
- [x] 写下 `dependencies` 和 `devDependencies` 的区别。

## 阶段 2：Docker 本地开发环境

### 学习目标

- [ ] 理解 Docker container、image、volume、network 的基本概念。
- [ ] 理解为什么数据库和缓存适合用 Docker 在本地启动。
- [ ] 理解环境变量如何连接应用和基础设施。

### 开发任务

- [ ] 编写 `docker-compose.yml`。
- [ ] 添加 PostgreSQL 服务。
- [ ] 添加 Redis 服务。
- [ ] 为 PostgreSQL 配置本地 volume。
- [ ] 编写 `.env.example`。
- [ ] 为 `apps/api` 配置数据库连接环境变量。
- [ ] 为 `apps/api` 配置 Redis 连接环境变量。

### 验收标准

- [ ] 可以通过 Docker 启动 PostgreSQL。
- [ ] 可以通过 Docker 启动 Redis。
- [ ] 可以用本地工具连接 PostgreSQL。
- [ ] 可以用本地工具连接 Redis。
- [ ] 停止并重启 Docker 后，PostgreSQL 数据仍然存在。

### 推荐练习

- [ ] 修改 PostgreSQL 端口并观察连接变化。
- [ ] 删除 volume 后重新启动，理解数据为什么会丢失。
- [ ] 写下 `.env` 与 `.env.example` 的区别。

## 阶段 3：NestJS API 基础

### 学习目标

- [ ] 理解 NestJS 的 Module、Controller、Service、DTO。
- [ ] 理解请求从 Controller 到 Service 的基本流转。
- [ ] 理解全局校验、异常处理、日志的作用。

### 开发任务

- [ ] 创建 `/health` 健康检查接口。
- [ ] 配置全局 validation pipe。
- [ ] 配置统一异常响应格式。
- [ ] 配置基础请求日志。
- [ ] 接入 Prisma。
- [ ] 创建 Prisma schema。
- [ ] 添加数据库迁移命令。
- [ ] 在 API 中验证 PostgreSQL 连接是否正常。

### 验收标准

- [ ] `GET /health` 返回服务状态。
- [ ] API 启动时可以连接 PostgreSQL。
- [ ] DTO 校验失败时返回清晰的错误信息。
- [ ] Prisma migration 可以成功执行。

### 推荐练习

- [ ] 新建一个简单的 `notes` 模块，包含创建和查询接口。
- [ ] 故意传入错误 DTO，观察校验错误。
- [ ] 故意关闭 PostgreSQL，观察 API 报错方式。

## 阶段 4：数据库与用户系统

### 学习目标

- [ ] 理解 SaaS 系统中用户、组织、成员关系的常见建模方式。
- [ ] 理解 JWT 鉴权的基本流程。
- [ ] 理解密码哈希和明文密码不能入库的原因。

### 开发任务

- [ ] 设计 `User` 模型。
- [ ] 设计 `Organization` 模型。
- [ ] 设计 `Membership` 模型。
- [ ] 实现用户注册接口。
- [ ] 实现用户登录接口。
- [ ] 使用安全哈希算法保存密码。
- [ ] 实现 JWT 签发。
- [ ] 实现鉴权 guard。
- [ ] 实现获取当前用户接口。
- [ ] 编写 seed 数据。

### 验收标准

- [ ] 用户可以注册。
- [ ] 用户可以登录并获得 token。
- [ ] 未登录用户无法访问受保护接口。
- [ ] 登录用户可以获取自己的用户信息。
- [ ] 一个用户可以属于一个或多个组织。

### 推荐练习

- [ ] 画出 User、Organization、Membership 的关系图。
- [ ] 手动解码 JWT，观察 payload 中包含什么。
- [ ] 尝试用错误密码登录，确认不会泄露敏感信息。

## 阶段 5：Next.js 前端基础

### 学习目标

- [x] 理解 Next.js App Router 的基本结构。
- [ ] 理解页面、布局、组件、服务端组件、客户端组件的差异。
- [ ] 理解前端如何保存和使用登录状态。

### 开发任务

- [ ] 搭建登录页。
- [ ] 搭建注册页。
- [ ] 搭建基础仪表盘页面。
- [ ] 创建 API 请求封装。
- [ ] 处理登录成功后的 token 保存。
- [ ] 实现前端路由保护。
- [ ] 实现组织切换 UI。
- [ ] 建立基础 UI 组件规范。
- [ ] 添加加载、空状态、错误状态。

### 验收标准

- [ ] 用户可以在前端完成注册。
- [ ] 用户可以在前端完成登录。
- [ ] 登录后可以进入仪表盘。
- [ ] 未登录访问仪表盘时会被引导到登录页。
- [ ] API 错误能在页面上被清楚展示。

### 推荐练习

- [ ] 比较服务端组件和客户端组件适合放哪些逻辑。
- [ ] 封装一个可复用的表单输入组件。
- [ ] 给登录流程画一个时序图。

## 阶段 6：Redis 与异步能力

### 学习目标

- [ ] 理解 Redis 适合做缓存、限流、验证码、队列辅助的原因。
- [ ] 理解同步请求和异步任务的区别。
- [ ] 理解任务队列在 SaaS 系统中的常见用途。

### 开发任务

- [ ] 在 NestJS 中接入 Redis。
- [ ] 实现一个 Redis 健康检查。
- [ ] 实现一个简单缓存示例。
- [ ] 引入 BullMQ。
- [ ] 创建后台任务队列。
- [ ] 实现一个异步任务提交接口。
- [ ] 实现任务状态查询接口。
- [ ] 为后续 AI Agent 任务预留队列结构。

### 验收标准

- [ ] API 可以连接 Redis。
- [ ] 可以写入并读取 Redis 缓存。
- [ ] 可以提交一个后台任务。
- [ ] 可以查询后台任务执行状态。
- [ ] 任务失败时有可读的错误状态。

### 推荐练习

- [ ] 把一个慢接口改造成异步任务。
- [ ] 设置缓存过期时间，观察过期后的读取结果。
- [ ] 故意让任务失败，观察重试或错误记录。

## 阶段 7：SaaS 核心业务模块

### 学习目标

- [ ] 理解多租户 SaaS 的组织、成员、角色、权限模型。
- [ ] 理解审计日志对企业系统的重要性。
- [ ] 理解订阅、套餐、用量限制的基本抽象。

### 开发任务

- [ ] 实现组织创建。
- [ ] 实现组织列表。
- [ ] 实现组织详情。
- [ ] 实现成员邀请。
- [ ] 实现成员角色管理。
- [ ] 实现基础 RBAC 权限判断。
- [ ] 设计订阅和套餐预留模型。
- [ ] 设计用量统计预留模型。
- [ ] 添加审计日志记录。
- [ ] 在前端补齐组织管理页面。

### 验收标准

- [ ] 用户可以创建组织。
- [ ] 用户可以邀请成员加入组织。
- [ ] 不同角色拥有不同操作权限。
- [ ] 关键操作会写入审计日志。
- [ ] 组织维度的数据不会互相串联。

### 推荐练习

- [ ] 列出 Owner、Admin、Member 三种角色的权限表。
- [ ] 设计一个简单套餐表，比如 Free、Pro、Team。
- [ ] 写下哪些操作必须记录审计日志。

## 阶段 8：AI Agent 模块

### 学习目标

- [ ] 理解 AI Agent 和普通聊天机器人的区别。
- [ ] 理解 conversation、message、tool call、memory 的基本概念。
- [ ] 理解为什么 AI 任务通常需要异步队列和状态追踪。

### 开发任务

- [ ] 设计 `Agent` 模型。
- [ ] 设计 `Conversation` 模型。
- [ ] 设计 `Message` 模型。
- [ ] 设计 `ToolCall` 模型。
- [ ] 实现创建会话接口。
- [ ] 实现发送消息接口。
- [ ] 实现消息列表接口。
- [ ] 将 AI 响应任务接入 BullMQ。
- [ ] 保存用户消息和 AI 消息。
- [ ] 预留工具调用接口。
- [ ] 预留接入 OpenAI API 或其他模型服务的 provider 层。
- [ ] 在前端实现最小可用 AI 对话页面。

### 验收标准

- [ ] 用户可以创建一个 AI 会话。
- [ ] 用户可以发送消息。
- [ ] 系统可以保存对话历史。
- [ ] AI 响应可以通过异步任务生成或模拟生成。
- [ ] 前端可以展示完整对话流。

### 推荐练习

- [ ] 先用 mock AI 响应跑通全流程，再接真实模型。
- [ ] 设计一个简单工具调用，比如获取当前组织信息。
- [ ] 记录一次 AI 响应从前端到队列再到数据库的完整链路。

## 阶段 9：测试、质量与安全

### 学习目标

- [ ] 理解单元测试、集成测试、端到端测试的区别。
- [ ] 理解输入校验、权限校验、限流、安全 headers 的作用。
- [ ] 理解提交前检查对长期项目维护的重要性。

### 开发任务

- [ ] 为 NestJS service 添加单元测试。
- [ ] 为关键 API 添加 e2e 测试。
- [ ] 为前端登录流程添加测试。
- [ ] 添加接口权限测试。
- [ ] 添加输入校验测试。
- [ ] 添加基础限流。
- [ ] 配置安全 headers。
- [ ] 添加提交前检查命令。
- [ ] 添加 CI 预留配置。

### 验收标准

- [ ] 核心 API 测试可以通过。
- [ ] 登录、鉴权、多租户隔离有测试覆盖。
- [ ] 错误输入不会导致服务崩溃。
- [ ] 常用检查命令可以在本地一键运行。

### 推荐练习

- [ ] 给一个权限 bug 写失败测试，再修复它。
- [ ] 比较 mock 数据库和真实测试数据库的优缺点。
- [ ] 写一份本项目的安全检查清单。

## 阶段 10：部署与运维

### 学习目标

- [ ] 理解开发环境和生产环境的差异。
- [ ] 理解 Dockerfile、镜像构建、环境变量、数据库迁移的部署流程。
- [ ] 理解日志、监控、错误追踪在生产系统中的作用。

### 开发任务

- [ ] 为 `apps/web` 编写生产 Dockerfile。
- [ ] 为 `apps/api` 编写生产 Dockerfile。
- [ ] 编写生产环境变量清单。
- [ ] 设计数据库迁移执行流程。
- [ ] 添加结构化日志。
- [ ] 预留错误追踪方案。
- [ ] 预留监控指标方案。
- [ ] 编写部署说明文档。

### 验收标准

- [ ] 前端可以构建生产镜像。
- [ ] 后端可以构建生产镜像。
- [ ] 生产环境变量清单完整且不包含真实密钥。
- [ ] 部署前后数据库迁移流程清晰。
- [ ] 服务异常时可以通过日志定位问题。

### 推荐练习

- [ ] 手动构建一次前端和后端 Docker 镜像。
- [ ] 写一份从空服务器部署本项目的步骤。
- [ ] 模拟一次服务报错并通过日志定位原因。

## 长期维护清单

- [ ] 每完成一个阶段，更新本文件的勾选状态。
- [ ] 每新增一个重要模块，补充对应学习笔记。
- [ ] 每遇到一个不懂的概念，记录问题、答案和示例。
- [ ] 每完成一个可运行版本，打一个 Git tag。
- [ ] 每次重构前，先确认测试或手动验收步骤。
- [ ] 每次引入新依赖，记录引入原因和替代方案。

## 当前默认技术选型

- [ ] Monorepo：`pnpm workspace`
- [ ] Frontend：Next.js + TypeScript
- [ ] Backend：NestJS + TypeScript
- [ ] Database：PostgreSQL
- [ ] ORM：Prisma
- [ ] Cache / Queue：Redis + BullMQ
- [ ] Local Infrastructure：Docker Compose
- [ ] AI Provider：先 mock，后续接入 OpenAI API 或其他模型服务
